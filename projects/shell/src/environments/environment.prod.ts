export const environment = {
  production: true,
  mfe: {
    "mfeBasket": "https://{yourVercelDeployPath}.vercel.app",
    "mfeProfile": "https://{yourVercelDeployPath}.vercel.app",
    "mfePortalProfile": "\"https://{yourVercelDeployPath}.vercel.app"
  }
};
// This depends on a VERCEL production Environment - but we'd modify this.
// TODO | TOM KLEPETA: Figure out how this would be env vars?
